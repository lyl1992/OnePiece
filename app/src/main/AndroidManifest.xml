<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hbw.onepiece">

    <application
        android:name=".base.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_onepiece"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_onepiece"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.NiceDialogActivity"
            android:launchMode="singleTop" />

        <activity
            android:name=".ui.SmartRefreshActivity"
            android:launchMode="singleTop" />

        <activity
            android:name=".ui.ToFirstCharActivity"
            android:launchMode="singleTop" />

        <activity
            android:name=".ui.FastJsonActivity"
            android:launchMode="singleTop" />

        <activity
            android:name=".ui.EventBusActivity"
            android:launchMode="singleTop" />

        <activity android:name=".ui.EasyPermissionsActivity"
            android:launchMode="singleTop"/>

    </application>

    <uses-permission android:name="android.permission.INTERNET" />
    <!--允许应用程序改变网络连接状态-->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <!--允许应用程序获取网络信息状态-->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />

</manifest>